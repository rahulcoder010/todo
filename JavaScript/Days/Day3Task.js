// Day 3 Tasks - DSA

// Easy Level
// 1. Print "Hello, world!" to the console. 
console.log("Hello, world!");

// 2. Declare two variables and assign them initial values of your choice.
let num1 = 5;
let num2 = 10;

// 3. Calculate the sum of the two variables from the previous step and store the result in a new variable.
let sum = num1 + num2;

// 4. Print the result of the sum to the console.
console.log(sum);

// Medium Level
// 5. Create an array with 5 elements of your choice.
let arr = [1, 2, 3, 4, 5];

// 6. Print the third element of the array to the console.
console.log(arr[2]);

// 7. Add a new element to the end of the array.
arr.push(6);

// 8. Print the length of the array to the console.
console.log(arr.length);

// Hard Level
// 9. Create an object with 3 key-value pairs of your choice.
let obj = {
  name: "John",
  age: 25,
  city: "New York"
};

// 10. Print the value of one of the keys to the console.
console.log(obj.name);

// 11. Update the value of one of the keys in the object.
obj.age = 30;

// 12. Print the updated value to the console.
console.log(obj.age);